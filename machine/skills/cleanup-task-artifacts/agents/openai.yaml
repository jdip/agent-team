interface:
  display_name: "Cleanup Task Artifacts"
  short_description: "Clean only proven-safe completed task artifacts"
  default_prompt: "Use $cleanup-task-artifacts for this authorized cleanup."
policy:
  allow_implicit_invocation: false
