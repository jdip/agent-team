interface:
  display_name: "Standards Upgrade"
  short_description: "Apply the relevant Repository Standard delta"
  default_prompt: "Use $standards-upgrade for this repository."
policy:
  allow_implicit_invocation: false
