interface:
  display_name: "PR to Test"
  short_description: "Deliver and verify task changes on test"
  default_prompt: "Use $pr-to-test for this repository."
policy:
  allow_implicit_invocation: false
