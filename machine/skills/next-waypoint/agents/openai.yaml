interface:
  display_name: "Next Waypoint"
  short_description: "Continue this repository’s saved Wayfinder map"
  default_prompt: "Use $next-waypoint to continue the saved Wayfinder map."
policy:
  allow_implicit_invocation: false
